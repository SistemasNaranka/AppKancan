import { RouteObject } from "react-router-dom";
import ArticlesLayout from "./layouts/ReferenciasLayout";

const routes: RouteObject[] = [
  {
    path: "/muestras",
    element: <ArticlesLayout />,
  },
];

export default routes;
